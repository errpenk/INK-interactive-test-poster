// Dictionary for generative text
export const WORDS = [
    "VOID", "FLUX", "CORE", "DATA", "ECHO", "SCAN", 
    "GRID", "NULL", "ZERO", "FORM", "ZONE", "HACK", 
    "TYPE", "INK", "BIOS", "MODE", "VIEW", "PORT"
];

export const NOUNS = [
    "The algorithm", "A structure", "Visual noise", "The archive", 
    "System failure", "Digital decay", "Raw output", "Memory leak"
];

export const VERBS = [
    "observes", "corrupts", "displays", "encodes", 
    "renders", "filters", "obscures", "indexes"
];

export const ENDINGS = [
    "the invisible.", "manifesting form.", "without context.", 
    "in the matrix.", "beyond logic.", "static data.", "a new layer."
];

// Visual Settings
export const SETTINGS = {
    gridSpacing: 50,
    magnetRadius: 120
};
